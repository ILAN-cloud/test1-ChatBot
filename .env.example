
# === Core ===
OPENAI_API_KEY=                # clé OpenAI (chat + transcription Whisper)
OPENAI_CHAT_MODEL=gpt-4o-mini  # modèle par défaut
ALLOW_ORIGINS=*                # CORS (à restreindre en prod)
PUBLIC_BASE_URL=http://localhost:8000

# === Prompt de spécialisation ===
# Texte qui définit le rôle et le contexte de l'assistant pour ce client.
SPECIAL_PROMPT=Tu es l'assistant d'une pizzeria. Tu prends les commandes, tu gères les réservations et tu réponds aux questions simples (horaires, adresse).

# === Notification client ===
# Adresse mail du commerçant/restaurateur qui doit recevoir commandes et réservations
CLIENT_NOTIFICATION_EMAIL=monclient@exemple.com

# === SMTP (envoi d'e-mails) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=tonemail@gmail.com
SMTP_PASS=mot_de_passe_ou_token

# === TTS (Text-to-Speech) ===
# Choisissez le provider: "azure" (par défaut) ou "elevenlabs"
TTS_PROVIDER=azure

# Azure Speech Service
AZURE_TTS_KEY=
AZURE_TTS_REGION=westeurope
AZURE_TTS_VOICE=fr-FR-DeniseNeural

# ElevenLabs (optionnel)
ELEVEN_API_KEY=
ELEVEN_VOICE_ID=Rachel

# === Twilio (téléphone) ===
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=     # numéro Twilio attribué
TWILIO_PHONE_MAP={}      # mapping JSON { "+33XXXXXXXXX": "client@example.com" } si besoin
